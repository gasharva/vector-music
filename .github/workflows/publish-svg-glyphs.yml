name: Publish SvgScene prototype SVGs and OCR

on:
  workflow_dispatch:

concurrency:
  group: publish-svg-glyphs
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  publish-glyphs:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Build SvgScene and OCR diagnostics
        run: |
          dotnet build SvgScene/SvgScene.csproj --configuration Release
          dotnet build Tools/TextOcrDiagnostics/TextOcrDiagnostics.csproj --configuration Release

      - name: Export unique prototype SVGs
        shell: bash
        run: |
          set -euo pipefail
          rm -rf artifacts/glyphs-svg publish
          mkdir -p artifacts publish

          dotnet run \
            --project SvgScene/SvgScene.csproj \
            --configuration Release \
            --no-build \
            -- \
            Samples/kancheli-mimino/kancheli-mimino.svg \
            artifacts/glyph-scene.json \
            --classify-symbols \
            --export-glyphs-svg artifacts/glyphs-svg \
            | tee artifacts/glyph-svg-export.log

          test -f artifacts/glyphs-svg/manifest.json
          test -f artifacts/glyphs-svg/index.html

          glyph_count="$(find artifacts/glyphs-svg -maxdepth 1 -type f -name '*.svg' | wc -l)"
          echo "Unique SVG glyph prototypes: $glyph_count"
          test "$glyph_count" -gt 0

          (
            cd artifacts
            zip -qr glyphs-svg.zip glyphs-svg
          )
          test -s artifacts/glyphs-svg.zip

          cp -R artifacts/glyphs-svg publish/glyphs-svg
          cp artifacts/glyphs-svg.zip publish/glyphs-svg.zip
          cp artifacts/glyph-svg-export.log publish/glyph-svg-export.log

      - name: Generate OCR text diagnostics
        shell: bash
        run: |
          set -euo pipefail

          dotnet run \
            --project Tools/TextOcrDiagnostics/TextOcrDiagnostics.csproj \
            --configuration Release \
            --no-build \
            -- \
            Samples/kancheli-mimino/kancheli-mimino.svg \
            artifacts \
            | tee artifacts/ocr-run.log

          test -f artifacts/parser.ocr.svg
          test -f artifacts/parser.ocr.runs.svg
          test -f artifacts/parser.ocr.json
          grep -q 'Horizontal runs' artifacts/ocr-run.log

          cp artifacts/parser.ocr.svg publish/parser.ocr.svg
          cp artifacts/parser.ocr.runs.svg publish/parser.ocr.runs.svg
          cp artifacts/parser.ocr.json publish/parser.ocr.json
          cp artifacts/ocr-run.log publish/ocr-run.log

      - name: Upload downloadable workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: svgscene-kancheli-prototype-svgs-and-ocr
          path: publish/
          if-no-files-found: error
          retention-days: 30

      - name: Publish into semantic-artifacts branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: semantic-artifacts
          publish_dir: ./publish
          destination_dir: kancheli-mimino
          keep_files: true
          commit_message: "Update Mimino SvgScene glyphs and OCR diagnostics from ${{ github.sha }}"
